{
  "name": "aandolanjeevi.in",
  "version": "0.0.0",
  "private": true,
  "description": "Resource repository for public protests — links, guides, and archives.",
  "type": "module",
  "scripts": {
    "build": "eleventy && pagefind --site _site",
    "serve": "eleventy --serve",
    "archive": "node scripts/archive-wayback.js",
    "archive-packages": "node --env-file=.env scripts/archive-packages.js",
    "check-links": "node scripts/check-links.js",
    "fonts": "node scripts/fetch-fonts.js"
  },
  "license": "CC-BY-NC-SA-4.0",
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "pagefind": "^1.3.0",
    "@11ty/eleventy-plugin-rss": "^2.0.0",
    "js-yaml": "^4.1.0"
  }
}
